# Included by Synology nginx from conf.d/http.*.conf inside the http block.
limit_req_zone $binary_remote_addr zone=r2archive_per_ip:10m rate=20r/s;
limit_conn_zone $binary_remote_addr zone=r2archive_conn_per_ip:10m;
limit_req_zone $binary_remote_addr zone=practice_auth_per_ip:10m rate=5r/m;
